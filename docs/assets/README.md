# Screenshots

The README references three captures from this directory. They are placeholders
until the real ones are added, and the README says so beneath them.

| File | What to capture |
| --- | --- |
| `landing.png` | The landing page with the assistant launcher at the bottom right |
| `answer.png` | A cited answer with its numbered "Where this comes from" list open |
| `voice.png` | Voice mode: the microphone, the listening state, the spoken-answer indicator |

Capture at 2x on a light background, and crop to the panel rather than the whole
desktop. Nothing in a screenshot may show a real address: the sign-in field must
be empty or show an `@example.invalid` address.
